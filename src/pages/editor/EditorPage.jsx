// Phase 0 stub — preserves the current behavior of the /app route (linking out
// to the standalone prototype) until Phase 6 wires the React editor in place.
import EditorShell from './EditorShell'

export default function EditorPage() {
  return <EditorShell />
}
